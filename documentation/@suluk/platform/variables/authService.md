[**Suluk**](../../../README.md)

***

[Suluk](../../../packages.md) / [@suluk/platform](../README.md) / authService

# Variable: authService

> `const` **authService**: `object`

Defined in: [service.ts:199](https://github.com/MahmoodKhalil57/suluk/blob/aaf30cfa393544c0ef54e004d9e2f37412540fa8/tooling/ts/packages/platform/src/service.ts#L199)

## Type Declaration

### compose

> `readonly` **compose**: `object`

#### compose.exposes

> `readonly` **exposes**: `object`

#### compose.exposes.onUserCreated

> `readonly` **onUserCreated**: `object`

#### compose.exposes.onUserCreated.hookOptKey

> `readonly` **hookOptKey**: `"onUserCreated"` = `"onUserCreated"`

#### compose.exposes.onUserCreated.kind

> `readonly` **kind**: `"port"` = `"port"`

#### compose.exposes.onUserCreated.render

> `readonly` **render**: (`exprs`) => `string`

##### Parameters

###### exprs

`string`[]

##### Returns

`string`

### deps

> `readonly` **deps**: \[`"better-auth"`, `"@better-auth/api-key"`, `"@better-auth/passkey"`, `"@suluk/better-auth"`\]

### env

> `readonly` **env**: \[\{ `generated`: `true`; `hint`: `` "session-signing key — AUTO-GENERATED by `bun run provision` (32 random bytes); no need to supply it" ``; `name`: `"BETTER_AUTH_SECRET"`; `required`: `true`; `secret`: `true`; \}, \{ `hint`: `"your deployed origin, e.g. https://api.example.com"`; `name`: `"BETTER_AUTH_URL"`; \}, \{ `hint`: `"optional — enables Google sign-in"`; `name`: `"GOOGLE_CLIENT_ID"`; `secret`: `true`; \}, \{ `hint`: `"optional — pairs with GOOGLE_CLIENT_ID"`; `name`: `"GOOGLE_CLIENT_SECRET"`; `secret`: `true`; \}\]

### id

> `readonly` **id**: `"auth"` = `"auth"`

### mount

> `readonly` **mount**: `object`

#### mount.from

> `readonly` **from**: `"./auth"` = `"./auth"`

#### mount.kind

> `readonly` **kind**: `"middleware"` = `"middleware"`

#### mount.symbol

> `readonly` **symbol**: `"mountAuthRoutes"` = `"mountAuthRoutes"`

### provision

> `readonly` **provision**: `object`

#### provision.from

> `readonly` **from**: `"./src/provision/auth"` = `"./src/provision/auth"`

#### provision.symbol

> `readonly` **symbol**: `"authProvision"` = `"authProvision"`

### serviceOpts

> `readonly` **serviceOpts**: [`Schema`](../interfaces/Schema.md)\<[`AuthServiceOpts`](../interfaces/AuthServiceOpts.md)\>

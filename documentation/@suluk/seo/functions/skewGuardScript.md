[**Suluk**](../../../README.md)

***

[Suluk](../../../packages.md) / [@suluk/seo](../README.md) / skewGuardScript

# Function: skewGuardScript()

> **skewGuardScript**(`opts?`): `string`

Defined in: [skew.ts:26](https://github.com/MahmoodKhalil57/suluk/blob/fe6f559442a0c0faed05a49bbb88d075a56ea812/tooling/ts/packages/seo/src/skew.ts#L26)

Inline client guard (drop into a <script>). Detects a newer deploy and converts the next same-origin link
 click into a full page load so the user lands on the new version cleanly.

## Parameters

### opts?

[`SkewGuardOptions`](../interfaces/SkewGuardOptions.md) = `{}`

## Returns

`string`

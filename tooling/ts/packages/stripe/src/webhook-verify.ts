/**
 * @deprecated MOVED to @suluk/payments (C048). Re-export shim — import `verifyStripeSignature` / `timingSafeHexEqual`
 * from `@suluk/payments` instead.
 */
export { verifyStripeSignature, timingSafeHexEqual, type VerifyOptions } from "@suluk/payments";

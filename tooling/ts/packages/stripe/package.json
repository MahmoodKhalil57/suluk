{
  "name": "@suluk/stripe",
  "version": "0.1.7",
  "description": "DEPRECATED — SUPERSEDED by @suluk/payments (the provider-agnostic seam, C048). The pricing primitives + webhook signature verification/router have MOVED there (this package now re-exports them as shims); use @suluk/payments for all payment flows, pricing, and webhooks. What remains here (usage-based Billing Meters, the Stripe checkout params, shipping/tax adapters) is Stripe-specific and unported — do NOT reach for it for new work. CANDIDATE tooling.",
  "deprecated": "Use @suluk/payments (C048). Pricing + webhook surfaces moved there; @suluk/stripe re-exports them as shims and is otherwise frozen.",
  "publishConfig": { "access": "public" },
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/MahmoodKhalil57/suluk.git",
    "directory": "tooling/ts/packages/stripe"
  },
  "homepage": "https://github.com/MahmoodKhalil57/suluk#readme",
  "bugs": "https://github.com/MahmoodKhalil57/suluk/issues",
  "type": "module",
  "main": "src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "dependencies": { "@suluk/cost": "workspace:^", "@suluk/payments": "workspace:^" },
  "peerDependencies": { "stripe": "^17.0.0 || ^18.0.0" },
  "peerDependenciesMeta": { "stripe": { "optional": true } },
  "devDependencies": { "@types/bun": "latest" },
  "scripts": { "test": "bun test", "typecheck": "tsc --noEmit -p ." }
}

{
  "name": "@suluk/deploy",
  "version": "0.3.0",
  "description": "Deploy a Suluk app behind a SWAPPABLE target interface — an EXECUTOR that ships over the target's API (no wrangler CLI) with credentials from the @suluk/env-decrypted .env. Cloudflare is the first provider (Workers + D1 + KV/R2 + static assets), an adapter over @suluk/cloudflare. CANDIDATE tooling.",
  "publishConfig": {
    "access": "public"
  },
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/MahmoodKhalil57/suluk.git",
    "directory": "tooling/ts/packages/deploy"
  },
  "homepage": "https://github.com/MahmoodKhalil57/suluk#readme",
  "bugs": "https://github.com/MahmoodKhalil57/suluk/issues",
  "type": "module",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@suluk/core": "workspace:^",
    "@suluk/cloudflare": "workspace:^"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@suluk/zod": "workspace:^",
    "zod": "^4.4.3"
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit -p ."
  }
}

# Configuration

## MountContractOptions

### Properties

#### authApi

wired from auth: a factory returning the Better-Auth `api` for the `/api/openapi.json` merge. Omit → the base doc.

**Type:** `(env: Bindings) => unknown`
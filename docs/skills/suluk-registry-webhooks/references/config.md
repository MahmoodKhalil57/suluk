# Configuration

## WebhookConfig

The endpoint signing secret (`whsec_…`), from env — declare `STRIPE_WEBHOOK_SECRET` in your `wrangler`/`.dev.vars`.

### Properties

#### signingSecret

**Type:** `string`

**Required:** yes
# Configuration

## MountReferenceOptions

### Properties

#### apiDocument

wired from contract (auto-injected — reference `requires: ["contract"]`): the v4 doc projector the page renders.

**Type:** `(principal?: { scopes: string[] }) => OpenAPIv4Document`

**Required:** yes
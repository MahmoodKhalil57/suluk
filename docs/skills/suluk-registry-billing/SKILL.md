---
description: Reference tooling for the OpenAPI v4.0 Suluk candidate.
name: suluk-registry-billing
---

# suluk

Reference tooling for the OpenAPI v4.0 Suluk candidate.

## Configuration

**AutoTopupConfig** — Auto-top-up config as the UI reads/writes it (defaults when unset). (3 options — see references/config.md)

## Quick Reference

32 exports (10 functions, 2 classes, 2 types, 18 constants) — see references/ for full API.

## References

Load these on demand — do NOT read all at once:

- When calling any function → read `references/functions.md` for full signatures, parameters, and return types
- When using a class → read `references/classes.md` for properties, methods, and inheritance
- When defining typed variables or function parameters → read `references/types.md`
- When using exported constants → read `references/variables.md`
- When configuring options → read `references/config.md` for all settings and defaults